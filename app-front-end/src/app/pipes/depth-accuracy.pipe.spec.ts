import { DepthAccuracyPipe } from './depth-accuracy.pipe';

describe('DepthAccuracyPipe', () => {
  it('create an instance', () => {
    const pipe = new DepthAccuracyPipe();
    expect(pipe).toBeTruthy();
  });
});
